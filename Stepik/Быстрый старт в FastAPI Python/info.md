http://localhost:8000/docs — здесь вы увидите интерфейс Swagger UI, с помощью которого можно отправлять запросы к вашему API.

http://localhost:8000/redoc — это альтернативный интерфейс для документации.


### Настройки проекта - https://stepik.org/lesson/1044666/step/8?unit=1053240

### Конечные точки в FastAPI - https://stepik.org/lesson/1044667/step/8?unit=1053241

# Операции
Операция — это выполнение конкретного HTTP-запроса к определённому пути.

### Основные:

GET — получение данных (например, список пользователей).

POST — создание нового ресурса (например, добавление нового пользователя).

PUT — полное обновление ресурса (например, замена всех данных пользователя).

DELETE — удаление ресурса.

### Менее распространённые:

OPTIONS — запрашивает, какие методы поддерживает сервер на этом URL.

HEAD — запрашивает заголовки ответа без тела (похож на GET, но без данных).

PATCH — частичное обновление ресурса (например, изменение только имени пользователя).

TRACE — отладочный метод, возвращающий полученный запрос обратно клиенту.

### Параметры пути (Path-параметры), Параметры тела запроса (Body-параметры), Параметры запроса (Query-параметры)
https://stepik.org/lesson/1044668/step/9?unit=1053242

